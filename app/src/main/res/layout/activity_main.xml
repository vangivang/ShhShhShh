<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <include
        android:id="@+id/appBar"
        layout="@layout/app_bar" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/appBar"
        android:paddingBottom="10dp">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <ImageView
                android:id="@+id/babyImage"
                android:layout_width="200dp"
                android:layout_height="wrap_content"
                android:layout_centerHorizontal="true"
                android:layout_marginTop="10dp"
                android:adjustViewBounds="true"
                android:scaleType="fitEnd"
                android:src="@drawable/baby_meme_b" />

            <View
                android:id="@+id/anchor"
                android:layout_width="50dp"
                android:layout_height="30dp"
                android:layout_below="@+id/babyImage"
                android:layout_centerInParent="true"
                android:layout_marginTop="50dp"
                android:visibility="invisible" />

            <Button
                android:id="@+id/startButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignBottom="@+id/anchor"
                android:layout_toLeftOf="@+id/anchor"
                android:text="Start" />

            <Button
                android:id="@+id/stopButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignBottom="@+id/anchor"
                android:layout_toRightOf="@+id/anchor"
                android:text="Stop" />

            <Spinner
                android:id="@+id/shutDownSpinner"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_below="@+id/anchor"
                android:layout_centerHorizontal="true"
                android:layout_marginTop="10dp"
                android:entries="@array/timer_values"
                />

            <Button
                android:id="@+id/deepWhiteNoiseButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Deep white noise"
                android:layout_centerHorizontal="true"
                android:layout_below="@+id/shutDownSpinner"
                android:layout_marginTop="15dp"/>

        </RelativeLayout>
    </ScrollView>

</RelativeLayout>